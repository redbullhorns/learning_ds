{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch LinkedList",
            "program": "${workspaceFolder}/linked_list/${input:programPath}",
            "console": "integratedTerminal"
        }
    ],
    "inputs": [
        {
            "type": "promptString",
            "id": "programPath",
            "description": "Path to your Python file (e.g., LinkedList.py)"
        }
    ]
}